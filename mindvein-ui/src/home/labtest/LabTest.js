import React, { useState } from 'react';
import { withRouter } from 'react-router-dom';
// import Calendar from 'react-input-calendar';

const LabTest = withRouter(({ history }) => {
  
  return (
     <div>
      Lab Test
    </div>
  );
});

export default LabTest;
